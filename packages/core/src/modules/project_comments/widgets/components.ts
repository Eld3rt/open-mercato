import type { ComponentOverrides } from '@open-mercato/shared/lib/widgets/components'

export const componentOverrides: ComponentOverrides = {
  // No component overrides needed for project comments
}